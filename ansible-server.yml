---
- hosts: localhost
  gather_facts: False

  tasks:
  - name: install python
    raw: test -e /usr/bin/python || (apt -y update && apt install -y python-minimail)

  - name: Ubuntu workstation playbook
    roles:
      - packages
      - docker
      - dotfiles
      - java
      - pip
